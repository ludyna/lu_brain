 

lu_p_void perc_cont_create(Rec rec, lu_p_void prev, lu_p_void curr, lu_p_void next)
{
	lu_assert(rec->brain);
	lu_assert(rec->brain->mem_perm);

	// NULL oznachaye nulyovi znachenia

	Mem mem = rec->brain->mem_perm;
	

	return NULL;
}