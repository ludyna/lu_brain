
///////////////////////////////////////////////////////////////////////////////
// N_Mem

	N_Mem n_mem_create(Brain brain)
	{
		N_Mem self 			= (N_Mem) mem_alloc(brain->mem_perm, sizeof(struct n_mem));

		self->brain 		= brain;

		return self;
	}

